#include <iostream>
using namespace std;

int main()
{
    /*
    int i = 5;  // 4Bt

    double d = 2.5;  //8Bt

    char c = 'C';  // 1Bt  код символа ASCII

    bool b = true;  // 1Bt  


    // неявні приведення типів

    int res = d;  //  2

    cout << res << endl;

    res = c;  // 67

    cout << res << endl;   // 67
    cout << c << endl;     // C

    res = b;  // 1

    cout << res << endl;

    res = i + d + c + b;

    cout << res << endl;  // 75

    double result = i + d + c + b;

    cout << result << endl;  //75.5


    c = 'c';

    res = int(c);  // явне приведення

    cout << res << endl;  // 99
    */



    // Оператори (дії над змінними, константами, літералами)

// 1) унарні  Застосовуються до 1 змінної  -,  ++,  --
    /*
    int number = 10;

    number = -number;   // -10

    cout << number << endl;

    // ++ Інкремент  збільшення змінної на 1

    int a = 2;

    a++;  // 3  // постфіксна форма: спочатку вираз, потім інкремент

    ++a;  // 4  // префіксна форма: спочатку інкремент, потім вираз

    a = a + 1;  // 5

    a += 1;  // 6

    cout << a << endl; // 6

    int b = a + ++a; // 14

    cout << b << endl;

    cout << a << endl;

    b = a++ + ++a;  // 16

    cout << b << endl;

    cout << a << endl;

    // -- Декремент: зменшує змінну на 1


    // 2) бінарні  Мають 2 будь-які операнди  +,-,*,/,%, >, < ...

    // 3) тернарний  Умовний оператор на 3 операнди
    */

    return 0;
}